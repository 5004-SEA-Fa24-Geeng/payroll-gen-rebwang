package student;

/**
 * This enum class stores the types of employee.
 */
public enum EmployeeType {
    /**
     * HOURLY employee's salary calculated based on hourly pay rate.
     */
    HOURLY,

    /**
     * SALARY employee's salary calculated based on annual pay rate divided by 24 pay terms.
     */
    SALARY;
}
